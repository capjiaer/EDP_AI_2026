# Replace hook for sub_step 'global_place' in pnr_innovus.place
Replaces the entire global_place() call.
Write your own implementation below.
# Fill in your code inside the proc body, or delete this file if not needed.

proc place_global_place_replace {} {
    global edp pnr_innovus pv_calibre

    # Your code here
}
