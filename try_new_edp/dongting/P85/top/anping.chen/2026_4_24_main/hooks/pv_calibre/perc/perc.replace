# Replace hook for sub_step 'perc' in pv_calibre.perc
Replaces the entire perc() call.
Write your own implementation below.
# Fill in your code inside the proc body, or delete this file if not needed.

proc perc_perc_replace {} {
    global edp pnr_innovus pv_calibre sta_pt

    # Your code here
}
