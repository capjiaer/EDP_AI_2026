services:
  edp-web:
    build:
      context: ..
      dockerfile: web/Dockerfile
    ports:
      - "5000:5000"
    environment:
      - EDP_CENTER=/app/resources
    volumes:
      - ../resources:/app/resources:ro
    restart: unless-stopped
